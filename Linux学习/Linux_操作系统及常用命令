login：
	用户名：用户ID

认证机制：Authentication
授权：Authorizaiton
审计：Audition

prompt ,命令行提示符
命令：
magic number ：魔数

#!/bin/bash

# command options ... orguments...

1.list :ls 列出，列表
	-l ：长格式
		文件类型：
			-：普通文件
			d：目录文件
			b：块设备（block）
			c：字符设备文件（character）
			l：符号链接文件（symbolic link file）
			p：命令管道（pipe）
			s：套接字文件（socket）
		文件权限：rwx
		文件硬链接的次数
		文件的属主（owner）
		文件的数组（group）
		文件大小（size），单位字节
		时间戳（timestamp）：最近一次被修改的时间
			访问：access
			修改：modify
			改变：change，metadata，元数据
	-a ：
	-l ：
	-d ：
	-t ：
文件系统：file sytem
目录：working directory，current directory

2.pwd
